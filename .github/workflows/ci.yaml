name: Service CI

on:
  pull_request:
      branches:
        - main

jobs:
  service-ci:
    uses: FA9-lab/platform-workflows/.github/workflows/node-service-ci.yml@v1.0.0
    with:
      node-version: '22'
